ETTERCAP MITM - QUICK REF

1.  sudo ettercap -G
2.  Hosts → Scan for hosts
3.  Hosts → Hosts list → set Target 1 + Target 2
4.  MITM → ARP poisoning → Sniff remote connections → OK
5.  Verify on victim: arp -a  (should show Kali's MAC)
6.  [optional] Wireshark filter: ip.addr == <t1> && ip.addr == <t2>
7.  MITM → Stop MITM attack(s)

[optional] If you set a static IP before the attack, restore with:
    sudo nmcli con mod "<connection>" ipv4.method auto && sudo nmcli con up "<connection>"
